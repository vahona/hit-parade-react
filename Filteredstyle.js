import React, { useContext } from 'react'
import { Context } from './Context'

function Filteredstyle() {

    const { song, stylefilter } = useContext(Context)

    return (

        <div>
            mmmm
        </div>
    )
}

export default Filteredstyle